import { Page } from "@playwright/test";

export const pageFixture = {
    // eslint-disable-next-line @typescript-eslint/ban-ts-comment
    //@ts-expect-error 
    //Error for Page needs to be fixed
    page: undefined as Page
}